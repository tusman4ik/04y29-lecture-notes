# Руководство по лабораторной работе

## 📋 Структура задачи

Лабораторная работа состоит из **3 частей**:

1. **Парсер файла** - написать парсер файла, где сообщения формата `ключ = значение`
2. **Структура данных** - написать свою структуру или класс Map с поиском за O(n) и некоторыми функциями
3. **Подстановка значений** - написать подстановку значений и вывести это всё в файл

## 🔧 Необходимые элементы

### Enum кодов возврата
```cpp
enum EReturnCode {
    SUCCESS,
    MISSING_KEY,
    INVALID_ARGS,
    IO_ERROR,
    TEMPLATE_SYNTAX_ERROR,
    OTHER_ERROR
};
```

**Важно:** Enum можно возвращать напрямую из main:
```cpp
int main(int argc, char* argv[]) {
    EReturnCode result = EReturnCode::SUCCESS;
    return result; // вернётся 0, другие коды от 1 до 5
}
```

## 📚 Функции из библиотек

### `<cstring>` - работа со строками

| Функция | Описание | Параметры | Возвращает |
|---------|----------|-----------|------------|
| `strlen(const char* str)` | Возвращает длину строки | `str` - указатель на строку | `size_t` - длина строки |
| `strcmp(const char* str1, const char* str2)` | Сравнивает две строки | `str1`, `str2` - строки для сравнения | `int` - 0 если равны, <0 если str1<str2, >0 если str1>str2 |
| `strncpy(char* dest, const char* src, size_t n)` | Копирует n символов из src в dest | `dest` - буфер назначения, `src` - источник, `n` - количество символов | `char*` - указатель на dest |
| `strchr(const char* str, int ch)` | Ищет первое вхождение символа в строке | `str` - строка, `ch` - искомый символ | `char*` - указатель на найденный символ или NULL |
| `strncmp(const char* str1, const char* str2, size_t n)` | Сравнивает первые n символов строк | `str1`, `str2` - строки, `n` - количество символов | `int` - результат сравнения |

### `<fstream>` - работа с файлами

| Функция/Метод | Описание | Параметры | Возвращает |
|---------------|----------|-----------|------------|
| `ifstream file(filename)` | Конструктор для чтения файла | `filename` - имя файла | - |
| `ofstream file(filename)` | Конструктор для записи в файл | `filename` - имя файла | - |
| `file.is_open()` | Проверяет, открыт ли файл | - | `bool` - true если файл открыт |
| `file.getline(buffer, size)` | Читает строку из файла | `buffer` - буфер, `size` - размер буфера | `istream&` - ссылка на поток |
| `file.read(buffer, size)` | Читает данные из файла | `buffer` - буфер, `size` - количество байт | `istream&` - ссылка на поток |
| `file.gcount()` | Возвращает количество прочитанных символов | - | `streamsize` - количество символов |
| `file.close()` | Закрывает файл | - | `void` |
| `outFile << data` | Записывает данные в файл | `data` - данные для записи | `ostream&` - ссылка на поток |

## 🛠️ Вспомогательные функции

Рекомендуется написать следующие функции для работы со строками:

```cpp
// Убирает лишние пробелы с начала и с конца строки
void trimString(char* line);

// Проверяет, является ли строка комментарием (начинается с # или //)
bool isComment(const char* line);
```

## 📖 Полезные ресурсы

- **Работа с файлами**: [Видео SimpleCode](https://www.youtube.com/watch?v=bTysglLJ8No)
- **Структуры в C++**: [Видео SimpleCode](https://www.youtube.com/watch?v=EWJqZN7ifI0)

## 🗂️ Структура данных Map

Пример реализации самописного класса Map:

```cpp
struct KeyValuePair {
    char key[101];      // Ключ (максимум 100 символов + '\0')
    char value[101];    // Значение (максимум 100 символов + '\0')
};

class Map {
private:
    int count;                          // Количество элементов
    KeyValuePair pairs[1024];           // Массив пар ключ-значение
    
public:
    Map();                              // Конструктор
    void add(const char* key, const char* value);        // Добавить/обновить пару
    const char* find(const char* key) const;             // Найти значение по ключу
    int getCount() const;                                // Получить количество элементов
};
```

## Важные замечания

1. **Безопасность**: Всегда проверяйте границы массивов и буферов
2. **Обработка ошибок**: Используйте enum кодов возврата для информирования об ошибках
3. **Память**: Не забывайте закрывать файлы после работы с ними
4. **Валидация**: Проверяйте корректность входных данных

## Задача 3

Для третьей части используйте знания из первых двух частей. Аккуратно реализуйте подстановку значений и **обязательно проверяйте длины файлов**, чтобы не выйти за границы буферов.